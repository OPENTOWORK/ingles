/**
 * Ejercicios locales desactivados (antes: 20 por tema/nivel generados en cliente).
 * Los ejercicios de Theory y Exam theory deben venir de Supabase
 * (levels_teoria_preguntas, levels_teoria_respuestas, levels_teoria_respuestas_abiertas).
 *
 * Los archivos *Exercises.js por tema conservan los pools por si se reutilizan más adelante.
 */
export function buildTheoryExercises(_slug, _config, _level = 'B2', _primaryLevel = 'B2') {
  return [];
}
